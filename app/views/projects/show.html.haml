- content_for :sidebar do
  = render :partial => "shared/sidebar/projects"

%h2= @project.class.to_s
= render :partial => "project", :object => @project

%br
%p= link_to "Back to all projects", projects_path
